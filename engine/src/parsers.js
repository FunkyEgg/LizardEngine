/**
 * Parses a renderingcontext so you get intellisense
 * @param {RenderingContext} context The rendering context
 * @returns {RenderingContext} The parsed rendering context
 */
export function parseRenderingContext(context) { return context; }

/**
 * Parses a webgl program
 * @param {WebGLProgram} program The unparsed WebGL Program
 * @returns {WebGLProgram} The webgl program parsed
 */
export function parseWebglProgram(program) { return program; }